// display.cpp - debug display functions (currently unused)
#include "display.hpp"
#include <stdio.h>

void render_keyboard_state(KeyboardState* kb_state) { (void)kb_state; }
void render_controller_state(uint8_t* state) { (void)state; }
void render_virtual_keyboard_state() {}
void render_device_section(DeviceState* device, int device_num) { (void)device; (void)device_num; }
void render_screen_update(DeviceState* device1, DeviceState* device2) { (void)device1; (void)device2; }
